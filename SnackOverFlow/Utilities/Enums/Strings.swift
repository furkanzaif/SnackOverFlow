import Foundation

enum StringItems: String {
    
    case facebookLogin = "Sign in with Facebook"
    case googleLogin = "Sign in with Google"
    case appleLogin = "Sign in with Apple"
    case emailLogin = "Sign in with email"
}
